# Claude Agent SDK credentials. Never committed; delivered out of band.
ANTHROPIC_API_KEY=

# Optional: override the model Laravel passes in (config acquisition.worker.model).
# TW_AGENT_MODEL=claude-opus-5

# Seconds one `php artisan acquisition:tool` call may take before the bridge gives up.
ACQUISITION_TOOL_TIMEOUT=600
